name: Cisco Digest (NotebookLM)

on:
  schedule:
    - cron: '0 23 * * 0'  # 08:00 JST Monday
  workflow_dispatch:

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install notebooklm-py

      - name: Run Cisco Digest (NotebookLM)
        env:
          NOTEBOOKLM_SESSION: ${{ secrets.NOTEBOOKLM_SESSION }}
        run: |
          python scripts/run_cisco_notebooklm_digest.py > /tmp/cisco_out.md 2>/tmp/cisco_err.txt
          EXIT=$?
          cat /tmp/cisco_err.txt >&2
          if [ $EXIT -eq 0 ] && head -1 /tmp/cisco_out.md | grep -q '^---'; then
            TODAY=$(python -c "from datetime import datetime, timezone, timedelta; print(datetime.now(timezone(timedelta(hours=9))).strftime('%Y-%m-%d'))")
            cp /tmp/cisco_out.md CiscoDigest/${TODAY}.md
            echo "Saved: CiscoDigest/${TODAY}.md"
          else
            echo "Script failed or output invalid — skipping"
            exit 1
          fi

      - name: Commit and push
        run: |
          git config user.email "cisco-digest-bot@github-actions"
          git config user.name "cisco-digest-bot"
          TODAY=$(python -c "from datetime import datetime, timezone, timedelta; print(datetime.now(timezone(timedelta(hours=9))).strftime('%Y-%m-%d'))")
          git add CiscoDigest/
          git diff --cached --quiet || git commit -m "cisco-digest(notebooklm): ${TODAY}"
          git push
